import { StyleSheet } from "react-native";

export const cstyle = StyleSheet.create({
    // text styles ==============
    textCenter: {
        textAlign: 'center'
    },

    // margin styles =============

    // padding styles ============

    //input box styles =================
    /* nameInput: {
        margin: 10,
        height: 30,
        backgroundColor: 'white',
        padding: 5,
        paddingLeft: 10,
        borderRadius: 50,
    }, */
});
