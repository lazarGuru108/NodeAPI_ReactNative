import { StyleSheet } from "react-native";

export const cstyle = StyleSheet.create({
    // text styles ==============
    textCenter: {
        textAlign: 'center'
    }

    // margin styles =============

    // padding styles ============
});
